# Scripts

Utilidades one-off para desarrollo. Pendientes:

- `load-free-exercise-db.ts` — Clona `yuhonas/free-exercise-db` y carga el JSON a `exercises`.
- `import-plan-csv.ts` — Importa `samples/rutina-demo-12-semanas.csv` como un plan para un cliente dado.
- `translate-exercises.ts` — Traduce `name_en` / `instructions` a ES usando OpenAI (opcional, requiere `OPENAI_API_KEY`).
